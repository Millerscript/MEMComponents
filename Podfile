workspace 'MEMComponents'
project 'Example/MEMComponents.xcodeproj'
use_frameworks!

platform :ios, '15.0'

target 'MEMComponentsExample' do
  pod 'MEMComponents', :path => './'
  pod 'Kingfisher', '~> 7.0'
  pod 'MEMBase', '0.1.3'
  pod 'MEMRest', '0.1.0'
  
  target 'MEMComponentsTests' do
    inherit! :search_paths

  end
end
